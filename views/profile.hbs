{{#each user}}
  <h2>{{name}}</h2>
  <img src="{{logo}}">
<form action="/post/{{id}}" method="POST">
  <p>Status update:</p><input type="text" name="post" placeholder="What's happening at {{name}}" autofocus/>
  <input type="submit" name="commit" value="Submit"/>
</form>

{{/each}}
